var prefixWrap = require("../src/main");
var assert = require("assert");

describe("PostCSS Prefix Wrap", function () {

  "use strict";

  it("should exist", function () {
    assert.notEqual(typeof prefixWrap, "undefined");
  });
});
